# GitHub action testing the code base by running the test suite

on:
  push:
    paths-ignore:
    - '.env'
    - '.gitignore'
    - '.paths'
    - 'COPYING'
    - 'README.md'
    - 'installer'
    - 'concept/docs/**'

jobs:
  test_suite:
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@v2
    - name: test suite
      run: |
        docker run \
        -v ${{github.workspace}}:/github \
        jmddk/concept:master /usr/bin/env bash -c ': \
            && cd / \
            && source ~/.bashrc \
            && concept -p params/example_params -n 2
        '

#            && rm -rf "${concept_dir}" \
#            && cp -r /github/concept "${concept_dir}" \
#            && concept -t nprocs_pp \
#&& concept -t all

