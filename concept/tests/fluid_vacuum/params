# Directory of this parameter file (non-parameter variable)
_this_dir = os.path.dirname(paths['params'])

# Input/output
initial_conditions = _this_dir + '/IC.hdf5'
output_dirs        = {'snapshot': _this_dir + '/output'}
output_bases       = {'snapshot': 'snapshot'}
output_times       = {'t': {'snapshot': linspace(0, 15*Gyr, 3),
                            'render2D': linspace(0, 15*Gyr, 26),
                            },
                      }
render2D_select    = {'all': {'terminal image': True}}

# Numerical parameters
boxsize    = 64*Mpc
φ_gridsize = 4*8  # Must be a multiple of 4

# Cosmology
t_begin = 0

# Physics
select_forces            = {'all': {'gravity': 'pm'}}
select_eos_w             = {'all': 'default'}
select_boltzmann_closure = {'matter fluid': 'truncate'}
select_approximations    = {'matter fluid': {'P = wρ': True}}

# Graphics
render2D_options = {
    'terminal resolution': φ_gridsize,
    'colormap': 'inferno',
    'enhance': False,
}

# Debugging options
enable_Hubble = False
